#include "main.h"

/**
 * print_numbers - prints numbers between 0 to 9.
 * Return: no return.
 */
void print_numbers(void)
{
	char c;

	for (ch = 0; ch < 9; c++)
	{
		_putchar(ch);
	}
	_putchar('\n');
}

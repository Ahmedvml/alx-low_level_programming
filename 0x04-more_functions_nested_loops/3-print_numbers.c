#include "main.h"

/**
 * print_numbers - prints numbers between 0 to 9.
 * Return: no return.
 */
void print_numbers(void)
{
	int c;

	for (ch = 0; ch < 10; c++)
	{
		_putchar(ch + '0');
	}
	_putchar(10);
}
